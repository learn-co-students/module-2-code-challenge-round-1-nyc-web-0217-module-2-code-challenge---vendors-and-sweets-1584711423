<h1>All Vendors</h1>

<h2><%="Average Price of Sweets: #{@price_avg} cents"%></h2>
<ul>
  <% @vendors.each do |vendor| %>
    <li><%= link_to vendor.name, vendor_path(vendor) %></li>
  <% end  %>
</ul>
