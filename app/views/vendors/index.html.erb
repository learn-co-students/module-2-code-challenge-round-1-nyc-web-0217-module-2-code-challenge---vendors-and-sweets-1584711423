<h1>All Vendors</h1>

<ul>
  <% @vendors.each do |vendor| %>
    <li><%= link_to vendor.name, vendor %></li>
  <% end  %>
</ul>

<!-- Average price per vendor -->
<!-- Dislpays next to vendor name -->
<h3>Average Price Per Vendor</h3>
  <% @vendors.each do |vendor| %>
    <%= vendor.name %>
    <%= vendor.average_price %> <br>
  <% end %>

